name: Salesforce CI Pipeline (Intelligent Coverage)

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    # ------------------------------------------
    # Checkout Code (FULL HISTORY REQUIRED)
    # ------------------------------------------
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # ------------------------------------------
    # Install Salesforce CLI
    # ------------------------------------------
    - name: Install SF CLI
      run: npm install -g @salesforce/cli

    # ------------------------------------------
    # Authenticate to Dev Org (Optional for retrieve)
    # ------------------------------------------
    - name: Authenticate Dev Org
      run: |
        echo "${{ secrets.JWT_SECRET_KEY }}" > server.key
        sf org login jwt \
          --username ${{ secrets.DEV_USERNAME }} \
          --jwt-key-file server.key \
          --client-id ${{ secrets.CONSUMER_KEY }} \
          --instance-url ${{ vars.INSTANCE_URL }} \
          --alias dev-org

    # ------------------------------------------
    # Detect Changed Files (MULTI-COMMIT SAFE)
    # ------------------------------------------
    - name: Detect Changed Files
      id: diffcheck
      run: |
        echo "Before: ${{ github.event.before }}"
        echo "After : ${{ github.sha }}"

        git diff --name-only ${{ github.event.before }} ${{ github.sha }} > changed_files.txt

        echo "Changed files:"
        cat changed_files.txt

    # ------------------------------------------
    # Extract Apex Changes
    # ------------------------------------------
    - name: Extract Changed Apex
      run: |
        grep -i "force-app/.*/classes/.*\.cls" changed_files.txt | sed 's#.*/##' | sed 's/.cls//g' > changed_classes.txt || true
        grep -i "force-app/.*/triggers/.*\.trigger" changed_files.txt | sed 's#.*/##' | sed 's/.trigger//g' > changed_triggers.txt || true

        echo "Changed Classes:"
        cat changed_classes.txt || true

        echo "Changed Triggers:"
        cat changed_triggers.txt || true

    # ------------------------------------------
    # Identify All Test Classes
    # ------------------------------------------
    - name: Identify Test Classes
      run: |
        grep -l "@isTest" force-app/main/default/classes/*.cls | sed 's#.*/##' | sed 's/.cls//g' > all_test_classes.txt || true

        echo "All Test Classes:"
        cat all_test_classes.txt || true

    # ------------------------------------------
    # Intelligent Test Mapping
    # ------------------------------------------
    - name: Map Tests (Class → Test Mapping)
      id: testmapping
      run: |
        > selected_tests.txt

        while read testclass; do
          FILE="force-app/main/default/classes/${testclass}.cls"

          # Skip if file not exists
          [ -f "$FILE" ] || continue

          # Match changed classes
          while read changedclass; do
            if grep -q "$changedclass" "$FILE"; then
              echo "$testclass" >> selected_tests.txt
            fi
          done < changed_classes.txt

          # Match triggers
          while read trigger; do
            if grep -q "$trigger" "$FILE"; then
              echo "$testclass" >> selected_tests.txt
            fi
          done < changed_triggers.txt

        done < all_test_classes.txt

        sort -u selected_tests.txt > final_tests.txt

        echo "Mapped Tests:"
        cat final_tests.txt || true

        TESTS=$(paste -sd ',' final_tests.txt)

        if [ -n "$TESTS" ]; then
          echo "tests=$TESTS" >> $GITHUB_OUTPUT
          echo "test-level=RunSpecifiedTests" >> $GITHUB_OUTPUT
        else
          echo "test-level=RunLocalTests" >> $GITHUB_OUTPUT
        fi

    # ------------------------------------------
    # Validation Rule Check (FORCE FULL TEST)
    # ------------------------------------------
    - name: Check Validation Rule Changes
      id: vrulecheck
      run: |
        if grep -i "validationRules" changed_files.txt; then
          echo "run_all=true" >> $GITHUB_OUTPUT
        else
          echo "run_all=false" >> $GITHUB_OUTPUT
        fi

    # ------------------------------------------
    # Build Delta Source
    # ------------------------------------------
    - name: Build Delta Source
      run: |
        mkdir -p delta_src

        while read file; do
          if [ -f "$file" ]; then
            mkdir -p "delta_src/$(dirname $file)"
            cp "$file" "delta_src/$file"
          fi
        done < changed_files.txt

        echo "Delta source prepared"

    # ------------------------------------------
    # Convert to MDAPI
    # ------------------------------------------
    - name: Convert to MDAPI
      run: |
        sf project convert source \
          --source-dir delta_src \
          --output-dir delta_mdapi

        echo "Converted to MDAPI"
        ls -R delta_mdapi

    # ------------------------------------------
    # Check Empty Delta
    # ------------------------------------------
    - name: Check Delta Empty
      id: checkdelta
      run: |
        if [ ! -f delta_mdapi/package.xml ]; then
          echo "no_changes=true" >> $GITHUB_OUTPUT
        else
          echo "no_changes=false" >> $GITHUB_OUTPUT
        fi

    # ------------------------------------------
    # Authenticate to UAT
    # ------------------------------------------
    - name: Authenticate UAT
      run: |
        echo "${{ secrets.JWT_SECRET_KEY }}" > server.key
        sf org login jwt \
          --username ${{ secrets.DEPLOYMENT_USERNAME }} \
          --jwt-key-file server.key \
          --client-id ${{ secrets.CONSUMER_KEYS }} \
          --instance-url ${{ vars.INSTANCE_URL }} \
          --alias uat-org

    # ------------------------------------------
    # VALIDATE DEPLOYMENT (NO DEPLOY)
    # ------------------------------------------
    - name: Validate Deployment to UAT
      if: ${{ steps.checkdelta.outputs.no_changes == 'false' }}
      run: |
        if [[ "${{ steps.vrulecheck.outputs.run_all }}" == "true" ]]; then

          echo "Running RunLocalTests due to validation rule changes"

          sf project deploy validate \
            --metadata-dir delta_mdapi \
            --target-org uat-org \
            --test-level RunLocalTests \
            --wait 30

        elif [[ "${{ steps.testmapping.outputs.test-level }}" == "RunSpecifiedTests" ]]; then

          echo "Running mapped test classes"

          sf project deploy validate \
            --metadata-dir delta_mdapi \
            --target-org uat-org \
            --test-level RunSpecifiedTests \
            --tests "${{ steps.testmapping.outputs.tests }}" \
            --wait 30

        else

          echo "Fallback to RunLocalTests"

          sf project deploy validate \
            --metadata-dir delta_mdapi \
            --target-org uat-org \
            --test-level RunLocalTests \
            --wait 30

        fi
